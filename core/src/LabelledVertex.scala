

/**
  * Created by katja on 17/11/15.
  */
class LabelledVertex(label: Int) extends VertexNeighbours[LabelledVertex] {
//  //override val neighbours: mutable.Set[LabelledVertex] = mutable.Set[LabelledVertex]()
//
//  override def toString = s"$label: ${sortedNeighbours.drop(1).foldLeft(sortedNeighbours.head.label.toString)((a,b) => s"$a, ${b.label}")}"
//  def addNeighbour(neighbour: LabelledVertex): Unit = neighbours += neighbour
//  def sortedNeighbours = neighbours.toSeq.sortWith(_.label < _.label)
}
